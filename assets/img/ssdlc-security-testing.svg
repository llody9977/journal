<svg viewBox="0 0 1200 560" xmlns="http://www.w3.org/2000/svg" font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif" role="img" aria-labelledby="ssdlc-title ssdlc-desc">
  <title id="ssdlc-title">SSDF practice groups, and the testing modalities compared by what each can observe</title>
  <desc id="ssdlc-desc">Two panels. The NIST SSDF practice groups — Prepare the Organization, Protect the Software, Produce Well-Secured Software, and Respond to Vulnerabilities — shown as four groups rather than a phase sequence. Below, the testing modalities compared by what each one can observe: static analysis reads code without running it, composition analysis reads the dependency graph, dynamic analysis exercises a running system from outside, interactive analysis observes execution from inside during tests, fuzzing generates inputs to reach unhandled states, and runtime self-protection acts in production.</desc>
  <rect width="1200" height="560" fill="#ffffff"/>

  <text x="50" y="46" font-size="26" font-weight="850" fill="#0f172a">No single tool sees the whole program</text>
  <text x="50" y="72" font-size="14" fill="#475569">Choose modalities by what each one can observe, and note that the SSDF itself is four practice groups — not a lifecycle sequence.</text>

  <rect x="40" y="96" width="1120" height="132" rx="14" fill="#eff6ff" stroke="#2563eb" stroke-width="2.5"/>
  <text x="62" y="120" font-size="11.5" font-weight="900" letter-spacing="1" fill="#2563eb">NIST SP 800-218 SSDF v1.1 — FOUR PRACTICE GROUPS, NOT PHASES</text>

  <rect x="62" y="136" width="262" height="76" rx="11" fill="#ffffff" stroke="#94a3b8" stroke-width="1.5"/>
  <text x="80" y="159" font-size="12.5" font-weight="800" fill="#0f172a">PO — Prepare the Organization</text>
  <text x="80" y="180" font-size="12" fill="#475569">Requirements, roles, toolchains,</text>
  <text x="80" y="199" font-size="12" fill="#475569">and securing the dev environment.</text>

  <rect x="340" y="136" width="262" height="76" rx="11" fill="#ffffff" stroke="#94a3b8" stroke-width="1.5"/>
  <text x="358" y="159" font-size="12.5" font-weight="800" fill="#0f172a">PS — Protect the Software</text>
  <text x="358" y="180" font-size="12" fill="#475569">Tamper protection and integrity</text>
  <text x="358" y="199" font-size="12" fill="#475569">verification for released software.</text>

  <rect x="618" y="136" width="262" height="76" rx="11" fill="#ffffff" stroke="#94a3b8" stroke-width="1.5"/>
  <text x="636" y="159" font-size="12.5" font-weight="800" fill="#0f172a">PW — Produce Well-Secured</text>
  <text x="636" y="180" font-size="12" fill="#475569">Design review, secure coding,</text>
  <text x="636" y="199" font-size="12" fill="#475569">review, testing, secure defaults.</text>

  <rect x="896" y="136" width="242" height="76" rx="11" fill="#ffffff" stroke="#94a3b8" stroke-width="1.5"/>
  <text x="914" y="159" font-size="12.5" font-weight="800" fill="#0f172a">RV — Respond to Vulns</text>
  <text x="914" y="180" font-size="12" fill="#475569">Identify, assess, remediate,</text>
  <text x="914" y="199" font-size="12" fill="#475569">and analyze for recurrence.</text>

  <rect x="40" y="244" width="1120" height="240" rx="14" fill="#f0fdfa" stroke="#0f766e" stroke-width="2.5"/>
  <text x="62" y="268" font-size="11.5" font-weight="900" letter-spacing="1" fill="#0f766e">TESTING MODALITIES — WHAT EACH ONE CAN AND CANNOT OBSERVE</text>

  <text x="62" y="294" font-size="12.5" font-weight="800" fill="#0f172a">SAST</text>
  <text x="180" y="294" font-size="12" fill="#475569">Reads code without running it. Exact file and line; no runtime, no config, no reachability. High false-positive rate.</text>

  <text x="62" y="322" font-size="12.5" font-weight="800" fill="#0f172a">SCA</text>
  <text x="180" y="322" font-size="12" fill="#475569">Reads the dependency graph. Known CVEs and licences; cannot tell whether the vulnerable path is reachable.</text>

  <text x="62" y="350" font-size="12.5" font-weight="800" fill="#0f172a">DAST</text>
  <text x="180" y="350" font-size="12" fill="#475569">Exercises a running deployment from outside. Sees deployed config and headers; no source location.</text>
  <text x="180" y="370" font-size="12" font-weight="700" fill="#9f1239">False positives still occur — observed is not the same as always right.</text>

  <text x="62" y="398" font-size="12.5" font-weight="800" fill="#0f172a">IAST</text>
  <text x="180" y="398" font-size="12" fill="#475569">Agent inside the runtime during functional tests. Line number plus runtime context; capped by test coverage.</text>

  <text x="62" y="426" font-size="12.5" font-weight="800" fill="#0f172a">Fuzzing</text>
  <text x="180" y="426" font-size="12" fill="#475569">Generates and mutates inputs, coverage-guided. Finds crashes and unhandled states; not logic or authorization flaws.</text>

  <text x="62" y="454" font-size="12.5" font-weight="800" fill="#0f172a">RASP</text>
  <text x="180" y="454" font-size="12" fill="#475569">Agent inside production, can block. Mitigates with real application context; the underlying flaw remains unfixed.</text>

  <rect x="40" y="500" width="1120" height="44" rx="12" fill="#f8fafc" stroke="#94a3b8" stroke-width="1.5"/>
  <text x="62" y="528" font-size="12.5" fill="#475569">Add <tspan font-weight="700" fill="#0f172a">secret scanning</tspan> over full history — rotation, not deletion, is the fix — and <tspan font-weight="700" fill="#0f172a">IaC scanning</tspan>, which catches the misconfigurations that become A02 in production.</text>
</svg>
